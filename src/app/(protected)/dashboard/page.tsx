import Dialog from "@/app/(protected)/dashboard/dialog";

export default function Dashboard() {
  return (
    <div className="flex h-[calc(100vh-145px)] flex-col items-center justify-center">
      <Dialog />
    </div>
  );
}
